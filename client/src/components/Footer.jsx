import React from 'react'
import { Button, Menu } from '@material-ui/core'

const Footer = (props) => {
  return (
    <Footer>
      <Button variant="outlined">
        New List
      </Button>
      <Button variant="outlined">
        Sort List
      </Button>
      
    </Footer>
    

    
  ) 
}